<!DOCTYPE html>
<html lang="es">
  <%- include("partials/head") %><!DOCTYPE html>

  <body>
    <%- include("partials/header") %>
    <main>
   <form class="login" action="/login" method="POST">
  <label for="email">Correo electrónico:</label>
        <input type="email"
         name="email" 
         value="<%= locals.old && old.email?old.email:"" %>"
          /> 
          <% if (locals.mensajeDeError &&
        mensajeDeError.email ) { %>
        <p class="error"><%- mensajeDeError.email.msg %></p>
        <% } %>
        <label for="password">ingresa tu contraseña:</label>
        <input type="password" name="password"/> <% if
        (locals.mensajeDeError && mensajeDeError.password ) { %>
        <p class="error"><%- mensajeDeError.password.msg %></p>
        <% } %>

        <button type="submit" value="Enviar" >iniciar sesión</button>
      </form>
    </main>
    <%- include("partials/footer") %>
  </body>
</html>
